// Placeholder for recommendation logic
export async function getRecommendations(symptom: string, userId?: string) {
  // Integrate with ML or rules engine here
  return [{ name: 'Paracetamol', symptom, userId }];
} 